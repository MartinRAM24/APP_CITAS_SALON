<%- include('partials/header') %>
<div class="card" style="max-width:460px;margin:auto;">
  <% if (tenant) { %>
    <div style="display:flex;align-items:center;gap:10px;margin-bottom:20px;">
      <span style="font-size:1.8rem;"><%= tenant.emoji %></span>
      <div>
        <div style="font-size:.75rem;color:#6b7280;text-transform:uppercase;letter-spacing:.05em;">Accediendo a</div>
        <div style="font-weight:600;color:<%= tenant.color %>"><%= tenant.nombre %></div>
      </div>
    </div>
  <% } %>

  <h2>Iniciar sesión</h2>
  <%- include('partials/flash') %>
  <form action="/login" method="POST">
    <label>Email</label>
    <input name="email" type="email" required />
    <label>Contraseña</label>
    <input name="password" type="password" required />
    <button type="submit">Entrar</button>
  </form>
  <p class="small">¿No tienes cuenta? <a href="/registro">Regístrate aquí</a></p>
  <p class="small"><a href="/">← Cambiar negocio</a></p>
</div>
<%- include('partials/footer') %>
